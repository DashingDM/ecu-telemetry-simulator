/*
 * compat.h — redirect snprintf to mini_snprintf, math to local impls
 */
#ifndef COMPAT_H
#define COMPAT_H

#include <stddef.h>
#include <stdint.h>
#include "printf.h"

/* Our snprintf/vsnprintf come from syscalls.c (already declared in include/stdio.h) */

#endif /* COMPAT_H */
